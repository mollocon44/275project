# 275project


#
mtsp_ga_multi_ch.m 6/5/17 does current GA versions; we can edit these at will, but might be best to create a new folder so that the filenames don't change.

mtsp_tour is optimizing for tour length only
mtsp_tour1 or whatever works standalone
mtsp_tour is commented out to integrate with main

main is currently commented out all over the place for debugging purposes

mtsp_tnt is optimizing for min(max tour + total distance)
It is currently working standalone